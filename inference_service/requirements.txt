# Inference-only dependencies (no training stack)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
numpy>=1.24.0
pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
# Optional; only needed when USE_TTA=true and CLAHE branch is enabled
opencv-python-headless>=4.8.0
